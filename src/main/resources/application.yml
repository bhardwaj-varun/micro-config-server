
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bhardwaj-varun/micro-config-server-data.git
          clone-on-start: true
server:
  port: 8085